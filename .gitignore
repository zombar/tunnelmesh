# Binaries
bin/
/tunnelmesh
/tunnelmesh-server
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
coverage.out
coverage.html
*.coverprofile

# Go workspace file
go.work

# Dependency directories
# vendor/ - Intentionally NOT ignored for supply chain security

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Config files (contain secrets)
server.yaml
peer.yaml
*.pem
*.key
*.crt
*.cer
*.der
*.p12
*.pfx

# Exception: docker test configs are OK to commit
!docker/config/server.yaml

# SSH keys (should not be committed)
id_ed25519
id_ed25519.pub
id_rsa
id_rsa.pub

# Log files
*.log

# Dynamic service discovery targets (generated by sd_generator)
monitoring/targets/peers.json

# Temporary files
tmp/
.tmp/

# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfvars
!terraform/terraform.tfvars.example
terraform/*.tfplan
terraform/crash.log
terraform/override.tf
terraform/override.tf.json
terraform/*_override.tf
terraform/*_override.tf.json
/tunnelmesh-benchmarker
internal/coord/web/coverage/
tunnelmesh_icon.png
