coverage:
  status:
    project:
      default:
        target: 40%
        threshold: 2%
    patch:
      default:
        target: 60%
comment:
  layout: "reach,diff,flags,files"
  behavior: default
